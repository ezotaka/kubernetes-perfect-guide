apiVersion: batch/v1
kind: Job
metadata:
  name: sample-multi-workqueue-job
spec:
  # 指定しない
  # completions: 1
  parallelism: 3
  backoffLimit: 1
  template:
    spec:
      containers:
      - name: tools-container
        image: ezotaka/tools:arm64
        command: ["sleep"]
        args: ["15"]
      restartPolicy: Never
